import { Component } from '@angular/core';

/**
 * En-tête simple affichant le logo MDD.
 * Utilisé sur les pages publiques (connexion, inscription).
 */
@Component({
  selector: 'app-header',
  templateUrl: './header.component.html',
})
export class HeaderComponent {}
